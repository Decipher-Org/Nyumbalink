import { StubPage } from "./StubPage";

export default function Login() {
  return (
    <StubPage
      title="Log in"
      body="Sign-in arrives in the next milestone, together with email verification and session handling."
    />
  );
}
